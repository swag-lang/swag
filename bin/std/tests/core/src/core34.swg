#test
{
    var tx = Core.Time.nowMilliseconds()
    /*
    var cpt = 0
    while cpt < 5
    {
        var t0 = Core.Time.nowMilliseconds()
        while(Core.Time.nowMilliseconds() - t0 < 1000);
        @print("tick\n");
        cpt += 1
    }*/
}
