/*
--- Part Two ---
Now find one that starts with six zeroes.
*/

using Core

func day4B()
{
    /*
    loop
    {
        let str = Format.toString("yzbqklnj%", #index)
        let v = Hash.Md5.compute(str)
        if v[0] == 0 and v[1] == 0 and v[2] == 0
        {
            //Console.print("total: ", #index)
            @assert(#index == 9962624)
            break
        }
    }
    */
}

