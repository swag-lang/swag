
#global testerror "Err0712"

struct ConvertFormatInt
{
}

// Convert an integer to an utf8 string, and put the result in a ConcatBuffer
func convertInt(fmt: ConvertFormatInt)
{
    var dataof = @dataof(toto)
    var toto: u64
}
