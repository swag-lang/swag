/*

This describes only the language itself, without the need of the 'Std.Core' module for example.

Things like dynamic arrays, dynamic strings, hash maps and so on are written in the Core module, so
you won't find them here.

*/