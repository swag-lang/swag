@kindof sur enum functions etc... renvoie le rawtype
----------------------
@interfaceof(any)
@interfaceof(pointer, typeinfo)
----------------------
test exhaustif sur 'is' (doit utiliser runtime comparetype())
----------------------
struct Point2
{
    using R = s32

    x, y: s32
}

//#[swag.printbc]
func(T) norm(vec: T)-> @typeof(T.R)
{

}