#import "std"

#test
{
    tx := std.time.nowMs()
    /*
    cpt := 0
    while cpt < 5
    {
        t0 := std.time.nowMs()
        while(std.time.nowMs() - t0 < 1000);
        @print("tick\n");
        cpt += 1
    }*/
}