#import "std"

#test
{
    tx := std.time.nowMilliseconds()
    /*
    cpt := 0
    while cpt < 5
    {
        t0 := std.time.nowMilliseconds()
        while(std.time.nowMilliseconds() - t0 < 1000);
        @print("tick\n");
        cpt += 1
    }*/
}