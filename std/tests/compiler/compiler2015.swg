struct(T) Array
{

}

impl Array
{
    func add(using self, value: T)
    {
    }

    func add(using self, values: const [..] T)
    {
    }
}

#test
{
    var arr: Array's32
    //loop 10 arr.add(@index)
}
