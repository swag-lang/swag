#import "core"
using core

#test
{
	/*var x: Array'u32
    x.resize(100)

    var v: swag.SystemAllocator
    //system.pushAllocator(v)
    {
        jobsystem.parallelFor(x.toSlice(), 1, null)
        {
            buffer[0] = 666
        }
    }

    loop x @assert(x[@index] == 666)*/
}