/*

Swag needs to know its home.

So the first thing to do is to open a console window, go to the directory where the executable is installed, and call 'swag env'.

This will set an environment variable called 'SWAG_FOLDER' with the path to the executable.

This must be done only once.

*/