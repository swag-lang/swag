

#test
{
    tx := core.Time.nowMilliseconds()
    /*
    cpt := 0
    while cpt < 5
    {
        t0 := core.Time.nowMilliseconds()
        while(core.Time.nowMilliseconds() - t0 < 1000);
        @print("tick\n");
        cpt += 1
    }*/
}