using core.random

#test
{
    var rng: Rng
    //rng.seedNow()
    /*console.printf("% ", rng.nextU32())
    console.printf("% ", rng.nextU64())
    console.printf("% ", rng.nextS32())
    console.printf("% ", rng.nextS64())
    console.printf("% ", rng.nextF32())*/
    console.printf("% ", rng.nextF64())
    console.printf("% ", rng.nextF64())
}