using core, image

//#[swag.printbc]
#test
{
    /*pathName := dataPath()
    files := try directory.enumerateFiles(pathName)
    visit f: files
    {
        img := try image.load(f.fullname)
    }*/
}